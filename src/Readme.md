# this is a readme file for writing the notes of the project
#created a store
#created a reducer
#created action creators
#then useSelector hook is used to access the state of the reducers
#the useDispatch hook is used to dispatch the actions
